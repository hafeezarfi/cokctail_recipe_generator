name: cocktail_recipe_generator
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.2.2
  google_fonts: ^6.1.0
  animated_bottom_navigation_bar: ^1.3.0
  firebase_core: ^2.19.0
  firebase_auth: ^4.11.1
  google_sign_in: ^6.1.5
  http: ^1.1.0
  equatable: ^2.0.5
  flutter_bloc: ^8.1.3
  email_validator: ^2.1.17
  flutter_svg: ^2.0.7
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
